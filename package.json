{
  "name": "workersssh",
  "version": "1.0.0",
  "description": "Cloudflare Workers SSH Tool - Browser-based SSH client",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --assets public --remote",
    "deploy": "node scripts/deploy.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "itty-router": "^5.0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
